import { FilterAirportsPipe } from './filter-airports.pipe';

describe('FilterAirportsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAirportsPipe();
    expect(pipe).toBeTruthy();
  });
});
